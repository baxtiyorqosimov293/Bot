services:
  - type: worker
    name: telegram-photo-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
    disk:
      name: bot-data
      mountPath: /var/data
      sizeGB: 1
